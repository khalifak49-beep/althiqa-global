@model OfferEditViewModel
@{ ViewData["Title"] = (L.IsArabic ? "إضافة عرض" : "Add offer"); }
<h1 class="fw-bold mb-3">@(L.IsArabic ? "إضافة عرض" : "Add offer")</h1>
<partial name="_Form" model="Model" />

@model OfferEditViewModel
@{ ViewData["Title"] = (L.IsArabic ? "تعديل عرض" : "Edit offer"); }
<h1 class="fw-bold mb-3">@(L.IsArabic ? "تعديل عرض" : "Edit offer")</h1>
<partial name="_Form" model="Model" />

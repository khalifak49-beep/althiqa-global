@model CouponEditViewModel
@{ ViewData["Title"] = (L.IsArabic ? "إضافة كوبون" : "Add coupon"); }
<h1 class="fw-bold mb-3">@(L.IsArabic ? "إضافة كوبون" : "Add coupon")</h1>
<partial name="_Form" model="Model" />

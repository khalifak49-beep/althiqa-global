@model CouponEditViewModel
@{ ViewData["Title"] = (L.IsArabic ? "تعديل كوبون" : "Edit coupon"); }
<h1 class="fw-bold mb-3">@(L.IsArabic ? "تعديل كوبون" : "Edit coupon")</h1>
<partial name="_Form" model="Model" />

@model WorkerEditViewModel
@{ ViewData["Title"] = (L.IsArabic ? "تعديل عاملة" : "Edit worker"); }
<h1 class="fw-bold mb-3">@(L.IsArabic ? "تعديل عاملة" : "Edit worker")</h1>
<partial name="_Form" model="Model" />

@model WorkerEditViewModel
@{ ViewData["Title"] = (L.IsArabic ? "إضافة عاملة" : "Add worker"); }
<h1 class="fw-bold mb-3">@(L.IsArabic ? "إضافة عاملة" : "Add worker")</h1>
<partial name="_Form" model="Model" />

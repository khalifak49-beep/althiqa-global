@{ ViewData["Title"] = L["common.access_denied_title"].ToString(); }
<section class="section">
    <div class="container text-center">
        <i class="bi bi-shield-lock text-danger" style="font-size:4rem;"></i>
        <h2 class="fw-bold mt-3">@L["common.access_denied_title"]</h2>
        <p class="text-muted">@L["common.access_denied_desc"]</p>
        <a class="btn btn-primary" asp-controller="Home" asp-action="Index">@L["common.go_home"]</a>
    </div>
</section>
